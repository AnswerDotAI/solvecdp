# SolveitCDP API


<!-- WARNING: THIS FILE WAS AUTOGENERATED! DO NOT EDIT! -->

``` python
from fastcore.test import test_eq
```

``` python
len(_cdp_domains), [d['domain'] for d in _cdp_domains[:5]]
```

    (55, ['Accessibility', 'Animation', 'Audits', 'Autofill', 'BackgroundService'])

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/solvecdp/blob/main/solvecdp/core.py#L27"
target="_blank" style="float:right; font-size:smaller">source</a>

### cdp_search

``` python

def cdp_search(
    q:str
):

```

*Search CDP domains and commands by name or description*

``` python
cdp_search('target')[:100]
```

    'Audits.checkFormsIssues: Runs the form issues check for the target page. Found issues are reported\nu'

solvecdp relies on the solveit-chrome extension being available - let’s
check it:

``` python
test_eq((await event_get_a('ext-ping')).result, 'pong')
```

Let’s try using the extension cdp directly:

``` python
r = await event_get_a('cdp-new-tab', url='https://example.com')
tid = r.result['tabId']
await asyncio.sleep(0.5)
tid
```

    895201191

We can add a little convenience for it:

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/solvecdp/blob/main/solvecdp/core.py#L43"
target="_blank" style="float:right; font-size:smaller">source</a>

### js_cdp

``` python

async def js_cdp(
    method, tabId:NoneType=None, params:VAR_KEYWORD
):

```

*Call self as a function.*

``` python
await js_cdp('Runtime.evaluate', tabId=tid, expression='document.title')
```

``` python
{'type': 'string', 'value': '6. Example Domain'}
```

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/solvecdp/blob/main/solvecdp/core.py#L52"
target="_blank" style="float:right; font-size:smaller">source</a>

### JsCDP

``` python

def JsCDP(
    tid
):

```

*Chrome DevTools Protocol via JS bridge with event support*

``` python
jc = await JsCDP.new()
```

We can subscribe to events thanks to the `solveit-chrome` extension
bridge:

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/solvecdp/blob/main/solvecdp/core.py#L100"
target="_blank" style="float:right; font-size:smaller">source</a>

### JsCDP.on

``` python

def on(
    events:VAR_POSITIONAL
):

```

*Call self as a function.*

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/solvecdp/blob/main/solvecdp/core.py#L97"
target="_blank" style="float:right; font-size:smaller">source</a>

### JsCDP.unsubscribe

``` python

async def unsubscribe(
    sub_id
):

```

*Call self as a function.*

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/solvecdp/blob/main/solvecdp/core.py#L84"
target="_blank" style="float:right; font-size:smaller">source</a>

### JsSub

``` python

def JsSub(
    cdp, events
):

```

*Initialize self. See help(type(self)) for accurate signature.*

``` python
async with jc.on('Page.loadEventFired') as sub:
    await jc('Page.navigate', url='https://example.com')
    evt = await sub()
```

``` python
await jc.close()
```

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/solvecdp/blob/main/solvecdp/core.py#L132"
target="_blank" style="float:right; font-size:smaller">source</a>

### JsCDPMethod

``` python

def JsCDPMethod(
    cdp, domain, method
):

```

*Initialize self. See help(type(self)) for accurate signature.*

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/solvecdp/blob/main/solvecdp/core.py#L123"
target="_blank" style="float:right; font-size:smaller">source</a>

### JsCDPDomain

``` python

def JsCDPDomain(
    cdp, domain
):

```

*Initialize self. See help(type(self)) for accurate signature.*

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/solvecdp/blob/main/solvecdp/core.py#L157"
target="_blank" style="float:right; font-size:smaller">source</a>

### JsCDP.eval

``` python

async def eval(
    expr
):

```

*Call self as a function.*

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/solvecdp/blob/main/solvecdp/core.py#L152"
target="_blank" style="float:right; font-size:smaller">source</a>

### JsCDP.wait_for_selector

``` python

async def wait_for_selector(
    sel, timeout:int=10
):

```

*Wait for CSS selector to match an element*

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/solvecdp/blob/main/solvecdp/core.py#L142"
target="_blank" style="float:right; font-size:smaller">source</a>

### JsCDP.wait_for

``` python

async def wait_for(
    expr, timeout:int=10
):

```

*Wait for JS expression to be truthy, return its value*

``` python
jc = await JsCDP.new()
```

``` python
await jc.page.navigate(url='https://example.com')
await jc.wait_for('document.title')
```

    'Example Domain'

``` python
await jc.close()
```

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/solvecdp/blob/main/solvecdp/core.py#L173"
target="_blank" style="float:right; font-size:smaller">source</a>

### JsCDP.goto

``` python

async def goto(
    url, kwargs:VAR_KEYWORD
):

```

*Navigate to url and wait for load+idle*

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/solvecdp/blob/main/solvecdp/core.py#L163"
target="_blank" style="float:right; font-size:smaller">source</a>

### JsCDP.wait_for_ready

``` python

async def wait_for_ready(
    timeout:int=10, idle_ms:int=500
):

```

*Wait until network is idle for idle_ms*

``` python
jc = await JsCDP.new()
```

``` python
await jc.goto('https://example.com')
await jc.eval('document.title')
```

    '6. Example Domain'

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/solvecdp/blob/main/solvecdp/core.py#L180"
target="_blank" style="float:right; font-size:smaller">source</a>

### JsCDP.pages

``` python

async def pages(
    
):

```

*Call self as a function.*

``` python
pages = await jc.pages
for p in pages[:3]: print(f"{p.get('tabId', '?'):>10} {p.get('title', '')[:60]}")
pg = first(p for p in pages if 'Example' in p['title'])
pg['url']
```

     895199784 8. Database Transactions — PlanetScale
     895200613 2. l:SolveIT - nbs◀️dialoghelper◀️aai-ws
     895199539 2. [AnswerDotAI/safepyrun] Run failed: Deploy to GitHub Page

    'https://example.com/'

``` python
jc2 = await JsCDP.new(pg['tabId'])
await jc2.eval('document.title')
```

    '5. Example Domain'

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/solvecdp/blob/main/solvecdp/core.py#L186"
target="_blank" style="float:right; font-size:smaller">source</a>

### JsCDP.screenshot

``` python

async def screenshot(
    
):

```

*Call self as a function.*

``` python
# await jc2.screenshot()
```

``` python
await jc2.close()
```

## LLMs and accessibility

``` python
page = await JsCDP.new(url='https://httpbin.org/forms/post')
await page.eval('document.title')
```

    '5. httpbin.org/forms/post'

``` python
await page.accessibility.enable()
tree = await page.accessibility.getFullAXTree()
len(tree)
```

    90

``` python
tree[0]
```

``` python
{ 'backendDOMNodeId': 14,
  'childIds': ['15'],
  'chromeRole': {'type': 'internalRole', 'value': 144},
  'frameId': '92354B6BB2A591B3DDB0267C24E21365',
  'ignored': False,
  'name': { 'sources': [{'attribute': 'aria-labelledby', 'type': 'relatedElement'}, {'attribute': 'aria-label', 'type': 'attribute'}, {'attribute': 'aria-label', 'superseded': True, 'type': 'attribute'}, {'nativeSource': 'title', 'type': 'relatedElement', 'value': {'type': 'computedString', 'value': '5. httpbin.org/forms/post'}}],
            'type': 'computedString',
            'value': '5. httpbin.org/forms/post'},
  'nodeId': '14',
  'properties': [{'name': 'focusable', 'value': {'type': 'booleanOrUndefined', 'value': True}}, {'name': 'url', 'value': {'type': 'string', 'value': 'https://httpbin.org/forms/post'}}],
  'role': {'type': 'internalRole', 'value': 'RootWebArea'}}
```

``` python
await page.close()
```

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/solvecdp/blob/main/solvecdp/core.py#L213"
target="_blank" style="float:right; font-size:smaller">source</a>

### AXTree

``` python

def AXTree(
    raw
):

```

*Chrome accessibility tree node with compact repr*

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/solvecdp/blob/main/solvecdp/core.py#L192"
target="_blank" style="float:right; font-size:smaller">source</a>

### AXNode

``` python

def AXNode(
    raw
):

```

*Chrome accessibility tree node with compact repr*

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/solvecdp/blob/main/solvecdp/core.py#L224"
target="_blank" style="float:right; font-size:smaller">source</a>

### build_ax_tree

``` python

def build_ax_tree(
    nodes
):

```

*Build AXNode tree from flat CDP accessibility node list*

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/solvecdp/blob/main/solvecdp/core.py#L243"
target="_blank" style="float:right; font-size:smaller">source</a>

### JsCDP.ax_tree

``` python

async def ax_tree(
    sid:NoneType=None
):

```

*Get accessibility tree for session*

``` python
page = await JsCDP.new(url='https://httpbin.org/forms/post')
```

``` python
root = await page.ax_tree()
root
```

<div class="prose">

- **RootWebArea** “5. httpbin.org/forms/post” `focusable=True`
  `url=https://httpbin.org/forms/post` \[#14\]
  - **LabelText** “” \[#20\]
    - **StaticText** “Customer name:” \[#62\]
      - **InlineTextBox** “Customer name:”
    - **textbox** “Customer name:” `focusable=True` `editable=plaintext`
      `settable=True` \[#2\]
  - **LabelText** “” \[#23\]
    - **StaticText** “Telephone:” \[#63\]
      - **InlineTextBox** “Telephone:”
    - **textbox** “Telephone:” `focusable=True` `editable=plaintext`
      `settable=True` \[#3\]
  - **LabelText** “” \[#26\]
    - **StaticText** “E-mail address:” \[#64\]
      - **InlineTextBox** “E-mail address:”
    - **textbox** “E-mail address:” `focusable=True`
      `editable=plaintext` `settable=True` \[#4\]
  - **group** “Pizza Size” \[#28\]
    - **Legend** “” \[#29\]
      - **StaticText** “Pizza Size” \[#65\]
        - **InlineTextBox** “Pizza Size”
    - **radio** ” Small” `focusable=True` \[#6\]
    - **radio** ” Medium” `focusable=True` \[#7\]
    - **radio** ” Large” `focusable=True` \[#8\]
  - **group** “Pizza Toppings” \[#36\]
    - **Legend** “” \[#37\]
      - **StaticText** “Pizza Toppings” \[#69\]
        - **InlineTextBox** “Pizza Toppings”
    - **checkbox** ” Bacon” `focusable=True` \[#9\]
    - **checkbox** ” Extra Cheese” `focusable=True` \[#10\]
    - **checkbox** ” Onion” `focusable=True` \[#11\]
    - **checkbox** ” Mushroom” `focusable=True` \[#12\]
  - **LabelText** “” \[#47\]
    - **StaticText** “Preferred delivery time:” \[#74\]
      - **InlineTextBox** “Preferred delivery time:”
    - **InputTime** “Preferred delivery time:” `focusable=True`
      `settable=True` \[#13\]
      - **spinbutton** “Hours Hours” `focusable=True` `settable=True`
        `valuemin=1` `valuemax=12` \[#51\]
        - **StaticText** “–” \[#75\]
          - **InlineTextBox** “–”
      - **StaticText** “:” \[#76\]
        - **InlineTextBox** “:”
      - **spinbutton** “Minutes Minutes” `focusable=True`
        `settable=True` `valuemax=59` \[#53\]
        - **StaticText** “–” \[#77\]
          - **InlineTextBox** “–”
      - **StaticText** ” ” \[#78\]
        - **InlineTextBox** ” ”
      - **spinbutton** “AM/PM AM/PM” `focusable=True` `settable=True`
        `valuemin=1` `valuemax=2` \[#55\]
        - **StaticText** “–” \[#79\]
          - **InlineTextBox** “–”
      - **button** “Show time picker” `focusable=True` `hasPopup=menu`
        \[#56\]
  - **LabelText** “” \[#58\]
    - **StaticText** “Delivery instructions:” \[#80\]
      - **InlineTextBox** “Delivery instructions:”
    - **textbox** “Delivery instructions:” `focusable=True`
      `editable=plaintext` `settable=True` `multiline=True` \[#5\]
  - **button** “Submit order” `focusable=True` \[#61\]
    - **StaticText** “Submit order” \[#81\]
      - **InlineTextBox** “Submit order”

</div>

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/solvecdp/blob/main/solvecdp/core.py#L263"
target="_blank" style="float:right; font-size:smaller">source</a>

### AXNode.find_all

``` python

def find_all(
    role:NoneType=None, name:NoneType=None
):

```

*Find all descendants matching role and/or name substring*

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/solvecdp/blob/main/solvecdp/core.py#L257"
target="_blank" style="float:right; font-size:smaller">source</a>

### AXNode.find_id

``` python

def find_id(
    role:NoneType=None, name:NoneType=None
):

```

*Find first descendant matching role and/or name substring*

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/solvecdp/blob/main/solvecdp/core.py#L250"
target="_blank" style="float:right; font-size:smaller">source</a>

### AXNode.find

``` python

def find(
    role:NoneType=None, name:NoneType=None
):

```

*Find first descendant matching role and/or name substring*

``` python
nmid = root.find_id('textbox', 'Customer name')
phid = root.find_id('textbox', 'Telephone')
nmid
```

    2

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/solvecdp/blob/main/solvecdp/core.py#L282"
target="_blank" style="float:right; font-size:smaller">source</a>

### JsCDP.click

``` python

async def click(
    backendNodeId, sid:NoneType=None
):

```

*Call self as a function.*

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/solvecdp/blob/main/solvecdp/core.py#L278"
target="_blank" style="float:right; font-size:smaller">source</a>

### JsCDP.js_node_run

``` python

async def js_node_run(
    code, backendNodeId, sid:NoneType=None
):

```

*Call self as a function.*

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/solvecdp/blob/main/solvecdp/core.py#L272"
target="_blank" style="float:right; font-size:smaller">source</a>

### JsCDP.js_node

``` python

async def js_node(
    fn, backendNodeId, sid:NoneType=None
):

```

*Call self as a function.*

``` python
await page.DOM.focus(backendNodeId=nmid)
await page.input.insertText(text='Jeremy Howard')

await page.DOM.focus(backendNodeId=phid)
await page.input.insertText(text='555-1234')
```

``` python
{}
```

``` python
await page.click(root.find_id('radio', 'Large'))
await page.click(root.find_id('checkbox', 'Extra Cheese'))
```

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/solvecdp/blob/main/solvecdp/core.py#L287"
target="_blank" style="float:right; font-size:smaller">source</a>

### JsCDP.fill_text

``` python

async def fill_text(
    backendNodeId, text, sid:NoneType=None
):

```

*Call self as a function.*

``` python
await page.fill_text(root.find_id('textbox', 'Delivery'), 'Ring the doorbell twice')
```

``` python
await page.js_node_run('this.value = "18:30"', root.find_id('InputTime', 'delivery time'));
```

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/solvecdp/blob/main/solvecdp/core.py#L293"
target="_blank" style="float:right; font-size:smaller">source</a>

### JsCDP.click_and_wait

``` python

async def click_and_wait(
    backendNodeId, kwargs:VAR_KEYWORD
):

```

*Click element and wait for load+idle*

``` python
await page.click_and_wait(root.find_id('button', 'Submit order'))
```

------------------------------------------------------------------------

<a
href="https://github.com/AnswerDotAI/solvecdp/blob/main/solvecdp/core.py#L299"
target="_blank" style="float:right; font-size:smaller">source</a>

### cdp_yolo

``` python

def cdp_yolo(
    
):

```

*Allow all CDP classes in safepyrun*
